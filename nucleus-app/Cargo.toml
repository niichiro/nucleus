[package]
name = "nucleus-app"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
yew = "0.21"
wasm-bindgen = "0.2"
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
gloo-net = "0.5"        # HTTP и WebSocket из WASM
yew-router = "0.18"
gloo-storage = "0.3"   # LocalStorage
